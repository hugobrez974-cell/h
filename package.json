{
  "name": "les-bungalows",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm install && node server.js",
    "build": "node frontend/package.json"
  }
}
